<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sản Phẩm - SportStore</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Karla:wght@400;600&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/15.7.1/nouislider.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body class="products-page">
    <main class="container">
        <div class="breadcrumb">
            <a href="/index.html">Trang chủ</a> / <span>Cửa hàng</span>
        </div>
        
        <!-- ++ THAY ĐỔI: Layout trang sản phẩm giờ chỉ còn 1 cột ++ -->
        <div class="products-page-content fade-in-element">
            
            <!-- ++ XÓA HOÀN TOÀN KHỐI <aside> TỪ ĐÂY ++ -->

            <section class="product-listing-v2">
                <div class="listing-header">
                    <h1 class="page-title">Cửa Hàng</h1>
                    <div class="listing-controls">
                        <button id="open-filter-btn" class="action-btn">
                            <i class="fas fa-filter" style="margin-right: 8px;"></i>
                            Bộ lọc
                        </button>
                        <span id="product-count-display"></span>
                        <select id="sort-by-filter" class="sort-by-select">
                            <option value="newest">Mới nhất</option>
                            <option value="price_asc">Giá: Tăng dần</option>
                            <option value="price_desc">Giá: Giảm dần</option>
                            <option value="rating_desc">Đánh giá cao</option>
                        </select>
                    </div>
                </div>

                <div id="active-filters-container" class="active-filters-container"></div>
                
                <div id="product-grid-wrapper" style="position: relative; min-height: 400px;">
                    <div id="product-grid-container" class="products-grid-v2"></div>
                    <div id="spinner-overlay" class="spinner-overlay hidden">
                        <div class="spinner"></div>
                    </div>
                </div>

                <div id="no-products-found-detailed" class="no-products-message" style="display: none;">
                    <i class="fas fa-search" style="font-size: 3rem; color: var(--color-border); margin-bottom: 1rem;"></i>
                    <h4>Không tìm thấy sản phẩm phù hợp</h4>
                    <p>Vui lòng thử lại với bộ lọc khác.</p>
                </div>
                <div id="pagination-container" class="pagination-container"></div>
            </section>
        </div>
    </main>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noUiSlider/15.7.1/nouislider.min.js"></script>
    <script type="module" src="/js/main.js"></script>
</body>
</html>






/* ==========================================================================
   SPORTSTORE - MAIN STYLESHEET (VERSION 2.6 - REFINED UX & POLISH)
   ========================================================================== */

/* ==========================================================================
   1. CSS RESET & CROSS-BROWSER COMPATIBILITY
   ========================================================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

html {
    scroll-behavior: smooth;
    line-height: 1.15;
    -webkit-text-size-adjust: 100%;
}

/* ==========================================================================
   2. GLOBAL STYLES & VARIABLES
   ========================================================================== */
:root {
    --color-primary-start: #4A90E2;
    --color-primary-end: #50E3C2;
    --color-text-light: #ffffff;
    --color-text-dark: #333333;
    --color-text-heading: #1a202c;
    --color-accent: #F5A623;
    --color-background-light: #f7f9fc;
    --color-background-main: #ffffff;
    --color-border: #e2e8f0;
    --color-footer-bg: #2c3e50;
    --font-main: 'Segoe UI', 'Karla', 'Roboto', -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
    --font-heading: 'Playfair Display', serif;
    --border-radius-main: 12px;
    --shadow-main: 0 8px 25px rgba(0, 0, 0, 0.08);
    --shadow-subtle: 0 4px 12px rgba(0, 0, 0, 0.05);
    --transition-fast: all 0.3s ease;
    --z-index-content: 1;
    --z-index-floating-button: 1000;
    --z-index-header: 1010;
    --z-index-overlay: 5000;
    --z-index-drawer: 5001;
    --z-index-modal: 5002;
    --z-index-toast: 9999;
}

body {
    font-family: var(--font-main);
    line-height: 1.6;
    color: var(--color-text-dark);
    background-color: var(--color-background-main);
    min-height: 100vh;
}

body.filter-is-open, body.cart-open, body.modal-is-open, body.menu-is-open {
    overflow: hidden;
}

.container { max-width: 1400px; margin: 0 auto; padding: 0 1.5rem; }
main { flex-grow: 1; }
a { color: var(--color-primary-start); text-decoration: none; transition: var(--transition-fast); }
a:hover { color: #357ABD; }

.loading-message, .no-products-message, .error-message { text-align: center; padding: 3rem 1rem; font-size: 1.2rem; color: #6c757d; }
.error-message { color: #e74c3c; }

/* ==========================================================================
   3. HEADER & SCROLL EFFECTS
   ========================================================================== */
.header-wrapper {
    position: sticky;
    top: 0;
    z-index: var(--z-index-header);
    transition: transform 0.4s ease-out;
    will-change: transform;
    background-color: #fff;
    isolation: isolate;
}
.header-wrapper.header-hidden {
    transform: translateY(-100%);
}

.top-bar {
    background-color: var(--color-footer-bg);
    color: rgba(255, 255, 255, 0.8);
    padding: 8px 1rem;
    font-size: 0.85rem;
    transition: all 0.3s ease;
}
.top-bar-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.top-bar-left, .top-bar-right {
    display: flex;
    gap: 1.5rem;
}
.top-bar a {
    color: rgba(255, 255, 255, 0.8);
    transition: var(--transition-fast);
}
.top-bar a:hover {
    color: #fff;
}
@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 8px rgba(245, 166, 35, 0.5);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 16px rgba(245, 166, 35, 0.9);
    transform: scale(1.02);
  }
}

/* Style cho dòng chữ khuyến mãi */
.promo-banner-text {
  display: inline-block; /* Để áp dụng padding và animation */
  background-color: var(--color-accent); /* Màu vàng cam từ biến màu có sẵn */
  color: var(--color-text-light); /* Chữ màu trắng */
  padding: 5px 15px; /* Tăng khoảng đệm để tạo thành "viên thuốc" */
  border-radius: 20px; /* Bo tròn các góc */
  font-weight: 600; /* Chữ đậm hơn một chút */
  text-transform: uppercase; /* Viết hoa toàn bộ */
  font-size: 0.85rem; /* Điều chỉnh kích thước chữ */
  letter-spacing: 0.5px; /* Tăng khoảng cách giữa các chữ cái */
  
  /* Áp dụng animation */
  animation: pulse-glow 2.5s infinite ease-in-out;
  transition: transform 0.3s ease;
}

/* Thêm một chút khoảng cách trên di động */
@media (max-width: 768px) {
  .top-bar-left {
    margin-bottom: 8px;
  }
}
.main-header {
    background-color: #fff;
    border-bottom: 1px solid var(--color-border);
    transition: all 0.3s ease;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
    height: 80px;
    transition: height 0.3s ease;
}

.logo {
    font-size: 28px;
    font-weight: 700;
    font-family: var(--font-heading);
    text-decoration: none;
    color: var(--color-text-heading);
    white-space: nowrap;
    display: flex;
    align-items: center;
}
.logo i {
    color: var(--color-primary-start);
    font-size: 24px;
}

.main-nav {
    flex-grow: 1;
    display: flex;
    justify-content: center;
}
.main-nav .nav-list {
    display: flex;
    gap: 1rem;
    list-style: none;
}
.main-nav .nav-list a {
    display: block;
    padding: 0.5rem 1rem;
    font-weight: 600;
    color: var(--color-text-dark);
    text-decoration: none;
    position: relative;
    transition: color 0.2s ease;
    white-space: nowrap;
}
.main-nav .nav-list a::after {
    content: '';
    position: absolute;
    bottom: -5px;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 2px;
    background-color: var(--color-primary-start);
    transition: width 0.3s ease;
}
.main-nav .nav-list a:hover {
    color: var(--color-primary-start);
}
.main-nav .nav-list a:hover::after {
    width: 100%;
}

.header-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
}
.header-search {
    position: relative;
    display: flex;
    align-items: center;
}
.header-search input {
    border: 1px solid var(--color-border);
    border-radius: 20px;
    padding: 8px 15px 8px 35px;
    width: 220px;
    transition: all 0.3s ease;
}
.header-search input:focus {
    width: 280px;
    border-color: var(--color-primary-start);
    box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.2);
    outline: none;
}
.header-search button {
    position: absolute;
    left: 12px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    color: #999;
}
.action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f0f2f5;
    color: var(--color-text-dark);
    border: none;
    border-radius: 20px;
    padding: 8px 15px;
    text-decoration: none;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 0.2s ease;
}
.action-btn:hover {
    background-color: #e4e6eb;
}
.cart-icon-btn {
    position: relative;
    font-size: 1.2rem;
    width: 40px;
    height: 40px;
    padding: 0;
    border-radius: 50%;
}
#cart-count-main {
    position: absolute;
    top: -2px;
    right: -2px;
    background: var(--color-accent);
    color: #fff;
    font-size: 12px;
    font-weight: bold;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
}
#auth-section, #user-info-container {
    display: contents;
}

body.scrolled .top-bar {
    visibility: hidden;
    opacity: 0;
    margin-top: -41px;
}
body.scrolled .header-content {
    height: 70px;
}
body.scrolled .main-header {
    box-shadow: var(--shadow-subtle);
}

/* ==========================================================================
   4. MEGA MENU & ANIMATIONS
   ========================================================================== */
.mega-menu-v2 {
    position: absolute;
    top: 100%;
    left: 50%;
    width: 1140px;
    max-width: 95vw;
    background-color: #fff;
    border-radius: var(--border-radius-main);
    box-shadow: var(--shadow-main);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.25s ease-out, transform 0.25s ease-out, visibility 0s 0.25s;
    z-index: 1100;
    overflow: hidden;
    display: grid;
    grid-template-columns: 2fr 2fr 2fr 3fr;
    gap: 2rem;
    padding: 2rem;
    transform-origin: top center;
    transform: translateX(-50%) scale(0.95) translateY(-10px);
}
.has-dropdown:hover .mega-menu-v2, .has-dropdown.is-open .mega-menu-v2 {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) scale(1) translateY(0);
    transition: opacity 0.25s ease-out, transform 0.25s ease-out;
}
.mega-menu-column {
    border-radius: 8px;
}
.mega-menu-heading {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text-heading);
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--color-border);
}
.mega-menu-links {
    list-style: none;
}
.mega-menu-links a {
    display: flex;
    align-items: center;
    padding: 0.7rem 0;
    color: #555;
    border-radius: 6px;
    transition: color 0.3s ease, padding-left 0.3s ease;
    position: relative;
}
.mega-menu-links a:hover {
    color: var(--color-primary-start);
    padding-left: 1.5rem;
}
.mega-menu-links a::before {
    content: '→';
    position: absolute;
    left: 0;
    font-weight: bold;
    color: var(--color-primary-start);
    opacity: 0;
    transform: translateX(-10px);
    transition: opacity 0.3s ease, transform 0.3s ease;
}
.mega-menu-links a:hover::before {
    opacity: 1;
    transform: translateX(0);
}
.mega-menu-banner a {
    display: block;
    position: relative;
    border-radius: 8px;
    overflow: hidden;
    height: 100%;
}
.mega-menu-banner img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
.mega-menu-banner a:hover img {
    transform: scale(1.1);
}
.mega-menu-banner span {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1.5rem 1rem;
    background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
    font-weight: 700;
    font-size: 1.2rem;
    text-align: center;
    color: white;
}

/* ==========================================================================
   5. REUSABLE COMPONENTS & ANIMATIONS
   ========================================================================== */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.fade-in-element {
    opacity: 0; /* Start hidden */
    animation: fadeInUp 0.8s ease-out forwards;
    animation-delay: var(--delay, 0s); /* Use custom property for delay */
}

.btn { display: inline-block; padding: 12px 25px; border-radius: 8px; border: none; cursor: pointer; font-weight: bold; transition: var(--transition-fast); text-align: center; }
.btn.primary-btn { background: var(--color-primary-start); color: white; }
.btn.primary-btn:hover { background: #357ABD; }
.btn-lg { padding: 15px 35px; font-size: 1.1rem; }

.section-title { font-size: 32px; font-weight: bold; text-align: center; margin-bottom: 40px; color: var(--color-text-heading); }

.products-grid, .products-grid-v2 {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 1.5rem;
    align-items: stretch;
}
.product-card-v2 {
    background: white;
    border-radius: var(--border-radius-main);
    border: 1px solid var(--color-border);
    overflow: hidden;
    transition: var(--transition-fast);
    position: relative;
    display: flex;
    flex-direction: column;
}
.product-card-v2:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-main);
}
.product-image-v2 {
    width: 100%;
    aspect-ratio: 1 / 1;
    background-color: #f9f9f9;
    padding: 1rem;
    position: relative;
    overflow: hidden;
    cursor: pointer;
}
.product-image-v2 img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.3s ease;
}
.product-card-v2:hover .product-image-v2 img {
    transform: scale(1.05);
}
.promo-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    background-color: var(--color-accent);
    color: white;
    padding: 4px 8px;
    font-size: 0.75rem;
    border-radius: 5px;
    font-weight: bold;
    z-index: 2;
}
.product-info-v2 {
    padding: 1rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
}
.product-brand-v2 {
    font-size: 0.8rem;
    color: #888;
    text-transform: uppercase;
    margin-bottom: 0.25rem;
}
.product-name-v2 {
    font-size: 1rem;
    font-weight: 500;
    margin-bottom: 1rem;
    line-height: 1.3;
}
.product-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
    padding-top: 1rem;
}
.product-price-v2 {
    font-size: 1.2rem;
    font-weight: 600;
}
.price-container {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
}
.product-price-v2.sale {
    color: #e74c3c;
}
.product-original-price-v2 {
    text-decoration: line-through;
    font-size: 0.9rem;
    opacity: 0.6;
}
.product-card-actions {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(4px);
    padding: 1rem;
    display: flex;
    gap: 10px;
    justify-content: center;
    transform: translateY(100%);
    transition: transform 0.3s ease;
    z-index: 2;
    border-top: 1px solid var(--color-border);
}
.product-card-v2:hover .product-card-actions {
    transform: translateY(0);
}
.product-card-actions .btn {
    flex-grow: 1;
    padding: 10px;
    font-size: 0.9rem;
}
.add-to-cart-icon-btn {
    background-color: var(--color-primary-start);
    color: white;
    border: none;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 1rem;
    transition: all 0.2s ease;
}
.add-to-cart-icon-btn:hover {
    background-color: #357ABD;
    transform: scale(1.1);
}

/* ==========================================================================
   6. HOMEPAGE SPECIFIC STYLES
   ========================================================================== */
.main-slideshow {
    position: relative;
    width: 100%;
    max-height: 500px;
    overflow: hidden;
    margin-bottom: 40px;
}
.carousel-inner {
    display: flex;
    width: 500%;
    transition: transform 0.5s ease-in-out;
    height: 100%;
}
.carousel-item {
    min-width: 20%;
    position: relative;
    background-color: #f0f0f0;
    height: 100%;
}
.carousel-item a {
    display: block;
    width: 100%;
    height: 100%;
}
.slideshow-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}
.carousel-control-prev,
.carousel-control-next {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    border: none;
    padding: 15px;
    cursor: pointer;
    z-index: 100;
    font-size: 1.5em;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.3s ease;
}
.carousel-control-prev:hover,
.carousel-control-next:hover {
    background-color: rgba(0, 0, 0, 0.8);
}
.carousel-control-prev { left: 10px; }
.carousel-control-next { right: 10px; }
.carousel-indicators {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 10px;
    z-index: 100;
}
.indicator {
    width: 8px;
    height: 8px;
    background-color: rgba(255, 255, 255, 0.6);
    border-radius: 50%;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
}
.indicator.active {
    background-color: #fff;
    transform: scale(1.2);
}

/* Other Homepage Sections */
.commitment-section { background-color: var(--color-background-main); padding: 2rem 1rem; border-bottom: 1px solid var(--color-border); }
.commitment-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; }
.commitment-item { display: flex; align-items: center; gap: 1.5rem; padding: 1rem; }
.commitment-item i { font-size: 2.5rem; color: var(--color-primary-start); }
.commitment-item h4 { font-size: 1.1rem; font-weight: 600; margin-bottom: 0.25rem; }
.commitment-item p { font-size: 0.9rem; color: #6c757d; }

.category-section { padding: 40px 1rem; background: var(--color-background-light); }
.category-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; margin-top: 30px; }
.category-card { background: #fff; padding: 30px; border-radius: var(--border-radius-main); text-align: center; transition: all 0.3s ease; cursor: pointer; box-shadow: var(--shadow-subtle); border: 1px solid var(--color-border); }
.category-card:hover { transform: translateY(-10px); box-shadow: var(--shadow-main); border-color: var(--color-primary-start); }
.category-title { font-size: 24px; font-weight: bold; margin-bottom: 10px; color: var(--color-text-heading); }
.category-subtitle { font-size: 1rem; color: #555; }

.tabs-header { text-align: center; margin-bottom: 40px; }
.product-tabs { display: inline-flex; background-color: #f0f2f5; border-radius: 30px; padding: 5px; margin-top: 1rem; }
.product-tab-btn { padding: 10px 25px; border: none; background-color: transparent; cursor: pointer; font-weight: 600; color: #555; border-radius: 25px; transition: all 0.3s ease; }
.product-tab-btn.active { background-color: #fff; color: var(--color-primary-start); box-shadow: 0 3px 10px rgba(0,0,0,0.1); }
.products-grid-container { position: relative; min-height: 300px; }

.brands-section { padding: 40px 1rem; background-color: var(--color-background-light); }
.brands-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 2rem; align-items: center; margin-top: 30px; }
.brand-logo { display: flex; justify-content: center; align-items: center; transition: var(--transition-fast); filter: grayscale(100%); opacity: 0.6; }
.brand-logo:hover { filter: grayscale(0%); opacity: 1; transform: scale(1.1); }
.brand-logo img { max-height: 50px; width: auto; max-width: 150px; }

.blog-section { padding: 40px 1rem; }
.blog-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; margin-top: 30px; }
.blog-post-card { background-color: #fff; border-radius: var(--border-radius-main); box-shadow: var(--shadow-subtle); overflow: hidden; transition: var(--transition-fast); display: flex; flex-direction: column; }
.blog-post-card:hover { transform: translateY(-5px); box-shadow: var(--shadow-main); }
.blog-post-image img { width: 100%; height: 200px; object-fit: cover; display: block; }
.blog-post-content { padding: 20px; display: flex; flex-direction: column; flex-grow: 1; }
.blog-post-category { display: inline-block; background-color: var(--color-accent); color: #fff; padding: 4px 10px; font-size: 0.75rem; font-weight: bold; border-radius: 20px; margin-bottom: 1rem; text-transform: uppercase; align-self: flex-start; }
.blog-post-title { font-size: 1.2rem; margin-bottom: 0.75rem; }
.blog-post-title a { color: var(--color-text-heading); }
.blog-post-title a:hover { color: var(--color-primary-start); }
.blog-post-excerpt { font-size: 0.9rem; color: #555; margin-bottom: 1rem; flex-grow: 1; }
.read-more-link { font-weight: bold; color: var(--color-primary-start); margin-top: auto; }

/* ==========================================================================
   7. UTILITIES & POPUPS
   ========================================================================== */
.page-overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background-color: rgba(0, 0, 0, 0.6);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.4s ease, visibility 0s 0.4s;
    z-index: var(--z-index-overlay);
}
body.overlay-is-active .page-overlay {
    opacity: 1;
    visibility: visible;
    transition: opacity 0.4s ease;
}

.auth-modal, .contact-modal, .cart-drawer {
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, transform 0.3s ease, visibility 0s 0.3s;
}
.auth-modal.is-visible, .contact-modal.is-visible, .cart-drawer.is-open {
    opacity: 1;
    visibility: visible;
    transition: opacity 0.3s ease, transform 0.3s ease;
}

.auth-modal, .contact-modal {
    position: fixed;
    top: 50%; left: 50%;
    z-index: var(--z-index-modal);
    padding: 2.5rem;
    border-radius: var(--border-radius-main);
    width: 90%; max-width: 450px;
    transform: translate(-50%, -50%) scale(0.95);
    background: white;
}
.auth-modal.is-visible, .contact-modal.is-visible {
    transform: translate(-50%, -50%) scale(1);
}

.close-auth-modal { position: absolute; top: 15px; right: 20px; font-size: 2rem; cursor: pointer; background: none; border: none; z-index: 10; }
.auth-modal-tabs { display: flex; border-bottom: 1px solid var(--color-border); margin-bottom: 2rem; }
.auth-tab-btn { flex: 1; padding: 1rem; font-size: 1.1rem; cursor: pointer; background: none; border: none; font-weight: 600; opacity: 0.6; border-bottom: 3px solid transparent; }
.auth-tab-btn.active { opacity: 1; border-bottom-color: var(--color-primary-start); color: var(--color-primary-start); }
.form-group { margin-bottom: 1.5rem; }
.form-group label { display: block; margin-bottom: 0.5rem; font-weight: 600; }
.form-group input, .form-group textarea, .form-group select { width: 100%; padding: 12px; border: 1px solid var(--color-border); border-radius: 8px; font-size: 1rem; transition: border-color 0.2s; }
.form-group input:focus, .form-group textarea:focus, .form-group select:focus { border-color: var(--color-primary-start); outline: none; box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.2); }
.form-error { color: #c0392b; text-align: center; min-height: 1.2em; margin-top: 1rem; }

.cart-drawer {
    position: fixed;
    top: 0; right: 0; width: 100%; max-width: 480px; height: 100%;
    background-color: #fff;
    transform: translateX(100%);
    display: flex; flex-direction: column;
    box-shadow: -10px 0 30px rgba(0,0,0,0.1);
    z-index: var(--z-index-drawer);
}
.cart-drawer.is-open {
    transform: translateX(0);
}

.cart-header { display: flex; justify-content: space-between; align-items: center; padding: 1rem 1.5rem; border-bottom: 1px solid var(--color-border); flex-shrink: 0; }
.cart-header h2 { font-size: 1.25rem; font-weight: 600; }
.close-cart-btn { background: none; border: none; font-size: 1.5rem; cursor: pointer; color: #888; transition: color 0.2s; }
.close-cart-btn:hover { color: var(--color-text-dark); }
.cart-content { flex-grow: 1; overflow-y: auto; position: relative; }
.cart-items-container { padding: 0.5rem 1.5rem; }
.cart-item-v2 { display: flex; gap: 1rem; padding: 1.25rem 0; border-bottom: 1px solid var(--color-border); }
.cart-item-image-v2 { width: 90px; height: 90px; object-fit: cover; border-radius: 8px; border: 1px solid var(--color-border); flex-shrink: 0; }
.cart-item-details-v2 { flex-grow: 1; display: flex; flex-direction: column; justify-content: space-between; }
.cart-item-info-v2 { margin-bottom: 0.5rem; }
.cart-item-name-v2 { font-weight: 600; color: var(--color-text-heading); display: block; margin-bottom: 0.25rem; }
.cart-item-name-v2:hover { color: var(--color-primary-start); }
.cart-item-options-v2 { font-size: 0.85rem; color: #666; }
.cart-item-footer-v2 { display: flex; justify-content: space-between; align-items: center; }
.item-quantity-controls-v2 { display: flex; align-items: center; border: 1px solid var(--color-border); border-radius: 6px; }
.item-quantity-controls-v2 button { background: none; border: none; padding: 4px 12px; cursor: pointer; font-size: 1rem; font-weight: 500; color: #888; }
.item-quantity-controls-v2 span { padding: 0 0.5rem; font-weight: 500; }
.cart-item-price-v2 { font-weight: 600; font-size: 1rem; }
.remove-item-btn-v2 { background: none; border: none; font-size: 1rem; color: #aaa; cursor: pointer; transition: color 0.2s; }
.remove-item-btn-v2:hover { color: #e74c3c; }
.empty-cart-view { display: flex; align-items: center; justify-content: center; height: 100%; text-align: center; }
.empty-cart-content { display: flex; flex-direction: column; align-items: center; gap: 1rem; color: #888; }
.empty-cart-content i { font-size: 4rem; color: var(--color-border); }
.empty-cart-content h4 { font-size: 1.2rem; font-weight: 600; color: var(--color-text-dark); }
.continue-shopping-btn { margin-top: 1rem; }
.cart-footer { padding: 1.5rem; border-top: 1px solid var(--color-border); background: #fff; box-shadow: 0 -5px 15px rgba(0,0,0,0.05); flex-shrink: 0; display: flex; flex-direction: column; gap: 1rem; }
.cart-summary { display: flex; justify-content: space-between; font-size: 1.1rem; font-weight: 600; }
.checkout-btn { width: 100%; padding: 1rem; font-size: 1.1rem; display: flex; justify-content: center; align-items: center; gap: 0.5rem; }

.floating-contact-btn { position: fixed; bottom: 30px; right: 30px; background: linear-gradient(45deg, var(--color-primary-start), var(--color-primary-end)); color: white; width: 60px; height: 60px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.8rem; box-shadow: 0 5px 15px rgba(0,0,0,0.2); text-decoration: none; z-index: var(--z-index-floating-button); transition: transform 0.3s ease; border: none; cursor: pointer;}
.floating-contact-btn:hover { transform: scale(1.1); }
.toast { position: fixed; bottom: 20px; left: 50%; transform: translate(-50%, 120px); background-color: #333; color: white; padding: 15px 25px; border-radius: 8px; z-index: var(--z-index-toast); transition: transform 0.4s ease; }
.toast.show { transform: translate(-50%, 0); }
.toast.success { background-color: #27ae60; }
.toast.error { background-color: #c0392b; }
.spinner-overlay { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(255, 255, 255, 0.7); display: flex; justify-content: center; align-items: center; z-index: 10; transition: opacity 0.3s; }
.spinner { border: 5px solid #f3f3f3; border-top: 5px solid var(--color-primary-start); border-radius: 50%; width: 50px; height: 50px; animation: spin 1s linear infinite; }
@keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
.hidden { display: none !important; }

/* Blur Effects */
body.overlay-is-active main,
body.overlay-is-active .footer {
    filter: blur(4px);
    transition: filter 0.4s ease;
}
body.menu-is-open main, body.menu-is-open .footer,
body.overlay-is-active .header-wrapper, body.overlay-is-active main, body.overlay-is-active .footer {
    pointer-events: none;
    user-select: none;
}

/* ==========================================================================
   8. PAGE-SPECIFIC LAYOUTS
   ========================================================================== */
.page-header { padding: 2rem 0; text-align: center; margin-bottom: 2rem; }
.page-header h1 { font-size: 2.5rem; margin-bottom: 0.5rem; }
.page-header p { font-size: 1.1rem; color: #6c757d; }
.card-section { background: #fff; padding: 1.5rem; border-radius: var(--border-radius-main); box-shadow: var(--shadow-subtle); margin-bottom: 1.5rem; }

/* Product List Page - Modern Filter UX */
.products-page-content { 
    /* Layout 1 cột để sản phẩm chiếm toàn bộ chiều rộng */
}

/* ++ NÂNG CẤP: Sidebar bộ lọc sẽ là drawer trên mọi thiết bị ++ */
.products-sidebar-v2 {
    position: fixed; 
    top: 0; 
    left: 0; 
    width: 340px; /* Tăng chiều rộng một chút */
    max-width: 90vw;
    height: 100vh;
    z-index: var(--z-index-drawer);
    background-color: #fff;
    box-shadow: 10px 0 40px -10px rgba(0, 0, 0, 0.2); /* Shadow đẹp hơn */
    transform: translateX(-100%);
    display: flex; 
    flex-direction: column;
    padding: 0; 
    border: none; 
    border-radius: 0;
    opacity: 0;
    visibility: hidden;
    /* Hiệu ứng chuyển động mượt mà hơn */
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94), opacity 0.4s ease, visibility 0s 0.4s;
}
.products-sidebar-v2.is-open {
    transform: translateX(0);
    opacity: 1;
    visibility: visible;
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94), opacity 0.4s ease;
}

/* ++ NÂNG CẤP: Header của drawer ++ */
.filter-drawer-header { 
    display: flex; 
    align-items: center; 
    justify-content: space-between; 
    padding: 1.25rem 1.5rem; 
    border-bottom: 1px solid var(--color-border); 
    flex-shrink: 0; 
}
.filter-drawer-header h3 {
    font-size: 1.25rem;
    color: var(--color-text-heading);
}

/* ++ NÂNG CẤP: Nút đóng 'x' tinh tế hơn ++ */
#close-filter-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f0f2f5;
    border: none;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    cursor: pointer;
    transition: all 0.2s ease;
    font-size: 1.2rem;
    color: #555;
    line-height: 1;
}
#close-filter-btn:hover {
    background: #e4e6eb;
    transform: rotate(90deg);
}


.filter-drawer-content { 
    flex-grow: 1; 
    overflow-y: auto; 
    padding: 0.5rem 0;
}
.products-sidebar-v2 .filter-card { 
    padding: 1rem 1.5rem 1.5rem; 
    border-bottom: 1px solid var(--color-border);
}
.products-sidebar-v2 .filter-card:last-child {
    border-bottom: none;
}
.filter-card .filter-title {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--color-text-dark);
}

/* ++ NÂNG CẤP: Footer cố định (sticky) của drawer ++ */
.filter-drawer-footer {
    display: flex;
    padding: 1rem 1.5rem;
    background-color: #fff;
    border-top: 1px solid var(--color-border);
    box-shadow: 0 -5px 15px -5px rgba(0,0,0,0.07);
    flex-shrink: 0;
}

#open-filter-btn { 
    display: inline-flex; 
}


.product-listing-v2 { flex-grow: 1; width: 100%; }
.listing-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem; }
.listing-header .page-title { margin: 0; font-size: 1.8rem; }
.listing-controls { display: flex; align-items: center; gap: 1.5rem; }
.sort-by-select { padding: 8px 12px; border: 1px solid var(--color-border); border-radius: 8px; background-color: #fff; }
.pagination-container { display: flex; justify-content: center; align-items: center; gap: 0.5rem; padding: 2rem 0; }
.pagination-btn { border: 1px solid var(--color-border); background-color: #fff; color: var(--color-text-dark); padding: 0.5rem 1rem; cursor: pointer; border-radius: 8px; transition: var(--transition-fast); font-size: 1rem; min-width: 40px; }
.pagination-btn:hover { background-color: var(--color-background-light); border-color: var(--color-primary-start); }
.pagination-btn.active { background-color: var(--color-primary-start); color: #fff; border-color: var(--color-primary-start); font-weight: bold; cursor: default; }

.filter-pills-wrapper { display: flex; flex-wrap: wrap; gap: 0.75rem; }
.filter-options-v2 label { margin: 0; display: inline-block; }
.filter-options-v2 input[type="checkbox"] { display: none; }
.filter-options-v2 input[type="checkbox"] + span { display: inline-block; padding: 8px 15px; border: 1px solid var(--color-border); border-radius: 20px; cursor: pointer; transition: all 0.2s ease; font-size: 0.9rem; font-weight: 500; }
.filter-options-v2 input[type="checkbox"]:hover + span { border-color: var(--color-primary-start); color: var(--color-primary-start); }
.filter-options-v2 input[type="checkbox"]:checked + span { background-color: var(--color-primary-start); border-color: var(--color-primary-start); color: white; }

/* ++ NÂNG CẤP: Giao diện thanh trượt giá ++ */
#price-slider .noUi-connect {
    background: var(--color-primary-start);
}
#price-slider .noUi-handle {
    border-radius: 50%;
    background: #fff;
    box-shadow: var(--shadow-subtle);
    border: 3px solid var(--color-primary-start);
    width: 24px;
    height: 24px;
    right: -12px;
    top: -9px;
    cursor: pointer;
}
#price-slider .noUi-handle:focus {
    outline: none;
}
#price-slider .noUi-handle::before,
#price-slider .noUi-handle::after {
    display: none;
}

.active-filters-container { display: flex; flex-wrap: wrap; gap: 0.75rem; padding-top: 1rem; padding-bottom: 1.5rem; border-bottom: 1px solid var(--color-border); margin-bottom: 1.5rem; }
.active-filters-container:empty { display: none; }
.filter-tag { display: inline-flex; align-items: center; background-color: var(--color-background-light); border: 1px solid var(--color-border); border-radius: 20px; padding: 0.4rem 1rem; font-size: 0.9rem; font-weight: 500; }
.filter-tag .remove-filter-btn { background: none; border: none; margin-left: 0.75rem; cursor: pointer; font-size: 1.1rem; color: #999; padding: 0; line-height: 1; }
.filter-tag .remove-filter-btn:hover { color: var(--color-text-dark); }
#clear-all-filters-btn { background: none; border: none; color: var(--color-primary-start); font-weight: 600; cursor: pointer; text-decoration: underline; }

/* Product Detail Page - PHIÊN BẢN ĐÃ SỬA LỖI & CẢI TIẾN */
.product-detail-grid { 
    display: grid; 
    /* Sửa đổi: Cột ảnh (5 phần) nhỏ hơn cột thông tin (6 phần) */
    grid-template-columns: 5fr 6fr; 
    gap: 3rem; 
    margin-bottom: 3rem; 
    align-items: start; 
}

.product-media { 
    display: flex; 
    /* Sửa đổi: Chuyển sang layout cột dọc (ảnh chính ở trên, thumbnail ở dưới) */
    flex-direction: column; 
    gap: 1rem;
    /* Cải tiến: Làm "dính" cột ảnh khi cuộn */
    position: sticky;
    top: 100px;
}

.thumbnail-gallery { 
    display: flex;
    /* Sửa đổi: Sắp xếp thumbnail theo hàng ngang */
    flex-direction: row; 
    justify-content: center; /* Căn giữa các thumbnail */
    flex-wrap: wrap;
    gap: 10px;
}

.thumbnail-item { 
    border: 2px solid var(--color-border); 
    border-radius: 8px; 
    cursor: pointer; 
    overflow: hidden; 
    transition: var(--transition-fast);
    /* Sửa đổi: Đặt kích thước cố định cho thumbnail */
    width: 80px;
    height: 80px;
    flex-shrink: 0; /* Ngăn thumbnail bị co lại */
}
.thumbnail-item img { 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
}
.thumbnail-item:hover, .thumbnail-item.active { 
    border-color: var(--color-primary-start); 
    box-shadow: 0 0 0 2px var(--color-primary-start); 
}

.main-image-container { 
    flex-grow: 1; 
    aspect-ratio: 1 / 1; 
    border: 1px solid var(--color-border); 
    border-radius: var(--border-radius-main); 
    display: flex; 
    align-items: center; 
    justify-content: center;
    /* Thêm: Nền nhẹ cho đẹp mắt & overflow để hiệu ứng không tràn ra ngoài */
    background-color: var(--color-background-light); 
    overflow: hidden;
}
.main-image-container img { 
    display: block; 
    max-width: 90%; 
    max-height: 90%; 
    object-fit: contain; 
    width: auto; 
    height: auto;
    /* Thêm: Hiệu ứng chuyển động mượt mà */
    transition: transform 0.3s ease;
}
/* Thêm: Hiệu ứng phóng to khi hover */
.main-image-container:hover img {
    transform: scale(1.05);
}

.product-details-main .product-title { font-size: 2rem; }
.product-meta { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; margin: 1rem 0; }
.info-blocks { border: 1px solid var(--color-border); border-radius: 10px; margin: 1.5rem 0; }
.info-block { padding: 1rem; border-bottom: 1px solid var(--color-border); display: flex; align-items: center; gap: 10px; }
.info-block i { color: var(--color-primary-start); }
.info-block:last-child { border: none; }
.product-tabs-container { margin-top: 3rem; }
.tab-buttons { display: flex; gap: 0.5rem; border-bottom: 1px solid var(--color-border); }
.tab-btn { padding: 1rem 1.5rem; cursor: pointer; background: none; border: none; border-bottom: 3px solid transparent; font-size: 1rem; font-weight: 500; }
.tab-btn.active { border-bottom-color: var(--color-primary-start); color: var(--color-primary-start); }
.tab-content { display: none; padding-top: 1.5rem; }
.tab-content.active { display: block; }
.specs-list li { display: flex; justify-content: space-between; padding: 10px; border-bottom: 1px solid var(--color-border); }
.specs-list li:nth-child(odd) { background: var(--color-background-light); }
.spec-key { font-weight: 600; }
.video-responsive-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; background: #000; }
.video-responsive-container iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
.reviews-layout { display: grid; grid-template-columns: 2fr 1fr; gap: 2rem; }
.review-item { border-bottom: 1px solid var(--color-border); padding-bottom: 1rem; margin-bottom: 1rem; }
.rating-component { display: flex; align-items: center; gap: 10px; margin: 5px 0; }
.rating-stars { color: var(--color-accent); }

/* Checkout Pages */
.placeorder-layout, .shipping-page .form-card { display: grid; grid-template-columns: 2fr 1fr; gap: 2rem; align-items: start; }
.form-card { max-width: 800px; margin: 0 auto; padding: 2rem; }
.order-summary-card .summary-line { display: flex; justify-content: space-between; padding: 0.75rem 0; border-bottom: 1px solid var(--color-border); }
.order-summary-card .total-line { font-weight: bold; font-size: 1.2rem; border-top: 2px solid var(--color-text-dark); margin-top: 1rem; }
.summary-cart-item { display: flex; align-items: center; gap: 1rem; padding: 0.75rem 0; border-bottom: 1px solid var(--color-border); }
.summary-cart-item:last-child { border-bottom: none; }
.summary-cart-item img { width: 60px; height: 60px; object-fit: cover; border-radius: 8px; }
.summary-cart-item .item-info { flex-grow: 1; }

/* ==========================================================================
   9. FOOTER
   ========================================================================== */
.footer {
    background-color: var(--color-footer-bg);
    color: rgba(255, 255, 255, 0.7);
    padding: 100px 1rem 20px;
    font-size: 0.9rem;
    line-height: 1.7;
    position: relative;
    overflow: hidden;
}
.footer::before {
    content: '';
    position: absolute;
    top: 0; left: 0; width: 100%; height: 80px;
    background-color: var(--color-background-main);
    border-bottom-left-radius: 100% 40px;
    border-bottom-right-radius: 100% 40px;
    transform: translateY(-1px);
}
.footer-grid, .footer-bottom { position: relative; z-index: 1; }
.footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 30px;
    padding-bottom: 40px;
    margin-bottom: 30px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.footer-section h3 {
    font-family: var(--font-heading);
    font-size: 1.2rem;
    color: var(--color-text-light);
    margin-bottom: 1.5rem;
}
.footer-section ul { list-style: none; }
.footer-section ul a { color: rgba(255, 255, 255, 0.7); transition: var(--transition-fast); }
.footer-section ul a:hover { color: #fff; }
.social-links { margin-top: 1.5rem; display: flex; gap: 15px; }
.social-links a { display: inline-flex; justify-content: center; align-items: center; width: 40px; height: 40px; background-color: rgba(255, 255, 255, 0.1); color: var(--color-text-light); border-radius: 50%; font-size: 1.1rem; transition: var(--transition-fast); }
.social-links a:hover { background-color: var(--color-primary-start); transform: translateY(-3px); }
.footer-bottom { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; padding-top: 20px; font-size: 0.85rem; color: rgba(255, 255, 255, 0.5); }
.footer-certs img { height: 35px; opacity: 0.8; transition: opacity 0.3s; }
.footer-certs img:hover { opacity: 1; }

/* ==========================================================================
   10. RESPONSIVE DESIGN
   ========================================================================== */
@media (max-width: 992px) {
    .main-nav { display: none; }
    .product-detail-grid, .placeorder-layout, .reviews-layout { grid-template-columns: 1fr; }
    .footer-grid { grid-template-columns: 1fr 1fr; }
    /* Cải tiến: Khi thu nhỏ màn hình, cột ảnh sẽ không bị "dính" nữa */
    .product-media {
        position: static;
    }
}

@media (max-width: 768px) {
    .header-content { height: 70px; }
    body.scrolled .header-content { height: 60px; }
    .header-search { display: none; }
    .top-bar-content { flex-direction: column; gap: 5px; text-align: center; }
    .footer-grid, .footer-bottom { grid-template-columns: 1fr; text-align: center; justify-content: center;}
    .social-links { justify-content: center; }
    .listing-header { flex-direction: column; align-items: flex-start; gap: 1rem; }
    .mega-menu-v2 {
        grid-template-columns: 1fr;
        max-height: 70vh;
        overflow-y: auto;
        width: calc(100% - 2rem);
        left: 1rem; right: 1rem;
        transform: translate(0, -10px) scale(0.95);
    }
    .has-dropdown:hover .mega-menu-v2, .has-dropdown.is-open .mega-menu-v2 {
        transform: translate(0, 10px) scale(1);
    }
    .mega-menu-banner { display: none; }
}

/* ==========================================================================
   11. NEW UX IMPROVEMENTS (Added in v2.6)
   ========================================================================== */

/* SKELETON LOADING EFFECT */
@keyframes shimmer {
    0% { background-position: -468px 0; }
    100% { background-position: 468px 0; }
}

.skeleton-card {
    background-color: #fff;
    border-radius: var(--border-radius-main);
    border: 1px solid var(--color-border);
    overflow: hidden;
}

.skeleton-card .skeleton-image {
    width: 100%;
    aspect-ratio: 1/1;
    background: linear-gradient(to right, #eeeeee 8%, #dddddd 18%, #eeeeee 33%);
    background-size: 800px 104px;
    animation: shimmer 1.5s linear infinite;
}

.skeleton-card .skeleton-info {
    padding: 1rem;
}

.skeleton-card .skeleton-line {
    height: 1em;
    border-radius: 4px;
    margin-bottom: 0.75rem;
    background: linear-gradient(to right, #eeeeee 8%, #dddddd 18%, #eeeeee 33%);
    background-size: 800px 104px;
    animation: shimmer 1.5s linear infinite;
}

.skeleton-card .skeleton-line.short { width: 40%; }
.skeleton-card .skeleton-line.medium { width: 70%; }


/* SCROLL TO TOP BUTTON */
.scroll-to-top-btn {
    position: fixed;
    bottom: 100px;
    right: 30px;
    background: rgba(0, 0, 0, 0.4);
    color: white;
    width: 45px;
    height: 45px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    z-index: var(--z-index-floating-button);
    transition: opacity 0.3s, transform 0.3s, visibility 0.3s;
    border: none;
    cursor: pointer;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px);
}
.scroll-to-top-btn.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}
.scroll-to-top-btn:hover {
    background: rgba(0, 0, 0, 0.6);
}


/* SPINNING ICON FOR LOADING STATES */
@keyframes fa-spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
.fa-spin {
  animation: fa-spin 1s infinite linear;
}